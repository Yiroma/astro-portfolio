---
type ContainerTag = "div" | "section" | "nav" | "ul" | "main" | "article";

interface Props {
  as?: ContainerTag;
  class?: string;
}

const { as: Tag = "div", class: className } = Astro.props;
---

<Tag class:list={["mx-auto max-w-7xl px-6 lg:px-8", className]}>
  <slot />
</Tag>
